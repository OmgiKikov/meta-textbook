version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1000:1000"
    env_file:
      - .env
    restart: unless-stopped
    container_name: meta-textbook

networks:
  default:
    name: meta-textbook-network
